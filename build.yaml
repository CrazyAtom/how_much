targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
      riverpod_generator:
        options:
          provider_name_suffix: 'Provider'
