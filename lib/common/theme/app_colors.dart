import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primary = Color(0xFF3182F6);
  static const primaryLight = Color(0xFF74A9F9);
  static const primaryDark = Color(0xFF1B64DA);

  // Background Colors
  static const background = Color(0xFFF1F1F1);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF2F4F6);

  // Text Colors
  static const textPrimary = Color(0xFF191F28);
  static const textSecondary = Color(0xFF8B95A1);
  static const textTertiary = Color(0xFFAEB5BC);

  // Status Colors
  static const error = Color(0xFFFF5D5D);
  static const success = Color(0xFF4CD964);
  static const warning = Color(0xFFFFBE16);
  static const info = primary;

  // Other Colors
  static const divider = Color(0xFFEEEEEE);
  static const disabled = Color(0xFFE5E7EC);
  static const overlay = Color(0x80191F28);

  // Gradient Colors
  static const gradientStart = Color(0xFF3182F6);
  static const gradientEnd = Color(0xFF74A9F9);
}
